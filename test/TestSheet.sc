import model.Decision

var r = scala.util.Random

r.nextInt(3)

for (a <- 1 to 20) {
  println(Decision(scala.util.Random.nextInt(3)))
}

for (a <- 1 to 20) {
  println(Decision(scala.util.Random.nextInt(3)))
}

